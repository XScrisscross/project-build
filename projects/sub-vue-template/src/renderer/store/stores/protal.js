const state = {
  protalInfo: null,
}

const getters = {}

const mutations = {
  setProtalInfo(state, data) {
    state.protalInfo = data
  },
}

const actions = {}

export default {
  namespaced: true,
  state,
  getters,
  mutations,
  actions,
}
